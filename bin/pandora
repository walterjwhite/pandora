#!/usr/bin/env expect

set PANDORA_KEY [exec cat "$::env(HOME)/.config/walterjwhite/pandora"]

set _email [exec secrets get -o=s $PANDORA_KEY email-address]
set _password [exec secrets get -o=s $PANDORA_KEY password]

spawn pianobar
expect "* Email:"
send $_email\r

expect "* Password:"
send $_password\r

interact
